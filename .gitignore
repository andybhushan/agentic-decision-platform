# --- Node / Vite ---
node_modules/
dist/
.vite/
*.log
npm-debug.log*
yarn-error.log*

# --- .NET ---
bin/
obj/
*.user
*.suo
.vs/

# --- Azure Functions / secrets ---
local.settings.json
.env
.env.local
.env.*.local
*.publishsettings

# --- OS / IDE ---
.DS_Store
Thumbs.db

# --- Build / deploy artifacts & secrets ---
build/.sql-admin-pw
build/tracesapi-publish/
build/tracesapi.zip
build/*.log
build/*.err

# --- generated build/data outputs (full) ---
build/
**/build/
out/
**/out/
*.tsbuildinfo

# --- private prep material, never share ---
docs/TECHNICAL-WALKTHROUGH-CHAD-RICHARD.md
docs/TECHNICAL-WALKTHROUGH-SCRIPT.md
docs/ADP-W3-BLOG-POST.html
docs/CTO-DEMO-PLAYBOOK.md
ADP-CONSOLE-INTEGRATION-BRIEF.md

# --- reference-only harvested copy of Richard Hogan's client-demo repo.
# --- Never sanitized: still titled "PROJECT IMAGINE", links a live IMAGINE
# --- Azure deployment, and contains real "Chubb" references. Not part of
# --- ADP's own build; local reference only, never tracked or shared.
apps/_harvest-client-demo/

# --- dead, superseded console scaffold (pre-2026-07-10 rebuild).
# --- Untouched since the initial commit; apps/console/ is the real one.
platform/console/
